# Multi-Step Reasoning Agent with Self-Checking

This project implements a structured reasoning agent that solves word problems using a
Planner → Executor → Verifier loop. The agent performs multi-step reasoning, validates its own answers, and only exposes short explanations to the user.

---

## How to Run Locally

## 1. Setup Environment
```bash
python -m venv venv
venv\Scripts\activate   # Windows
pip install -r requirements.txt 


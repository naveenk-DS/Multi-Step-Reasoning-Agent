# Reasoning Agent (Planner → Executor → Verifier)

A multi-step self-checking reasoning system using a pluggable LLM backend.

## Features
- Planner → Executor → Verifier architecture
- Retries on verification failure
- Pluggable LLM provider (OpenAI or Mock)
- CLI and HTTP server
- Full test suite

## Run CLI
